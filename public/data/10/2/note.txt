Although the problem looks complicated, it actually requires counting number of unique characters in the input.
Without concerning performance, I used JS Set (which can count differenet elements of an array, and has a size field)