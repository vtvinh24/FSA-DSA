let s = args[0];
let wordDict = args[1].split(" ");
/**
 * @param {string} s
 * @param {string[]} wordDict
 * @return {boolean}
 */
var wordBreak = function (s, wordDict) {
  // s: abcde, catsandog (f), catanddog (t)
  // wordDict: ["cat", "cats", "and", "sand", "dog"];
  
  // 

  return false;
};

return wordBreak(s, wordDict);

// applet

// apple, app, let

// t